/********************************************************************************
 * generated by de.acegen 1.5.6
 ********************************************************************************/




package com.anfelisa.todo.commands;

import de.acegen.Command;
import de.acegen.CustomAppConfiguration;
import de.acegen.IDaoProvider;
import de.acegen.ViewProvider;
import de.acegen.PersistenceHandle;
import de.acegen.Event;

import com.anfelisa.todo.data.ITodoToggleData;

@SuppressWarnings("unused")
public abstract class AbstractToggleTodoCommand extends Command<ITodoToggleData> {

	public AbstractToggleTodoCommand(IDaoProvider daoProvider, ViewProvider viewProvider, CustomAppConfiguration appConfiguration) {
		super("com.anfelisa.todo.commands.ToggleTodoCommand", daoProvider, viewProvider, appConfiguration);
	}

	protected void addSuccessOutcome(ITodoToggleData data) {
		data.addOutcome("success");
	}
	
	@Override
	public void addEventsToTimeline(ITodoToggleData data, PersistenceHandle timelineHandle) {
		if (appConfiguration.getConfig().writeTimeline()) {
			if (data.hasOutcome("success")){
				daoProvider.getAceDao().addEventToTimeline("com.anfelisa.todo.events.ToggleTodoSuccessEvent", data, timelineHandle);
			}
		}
	}
	
	@Override
	public void publishEvents(ITodoToggleData data, PersistenceHandle handle, PersistenceHandle timelineHandle) {
		if (data.hasOutcome("success")){
			new Event<ITodoToggleData>("com.anfelisa.todo.events.ToggleTodoSuccessEvent", viewProvider).publish(data.deepCopy(), handle, timelineHandle);
		}
	}
	
	@Override
	public void publishAfterCommitEvents(ITodoToggleData data, PersistenceHandle handle, PersistenceHandle timelineHandle) {
	}
	
}




/******* S.D.G. *******/



